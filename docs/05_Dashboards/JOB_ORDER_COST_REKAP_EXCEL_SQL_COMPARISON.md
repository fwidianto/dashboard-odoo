# Job Order Cost Rekap Excel SQL Comparison

The workbook `Ceking Project Indokordsa.xlsx` was not found in this repo or workspace.

Expected location:

```text
private_reference/Ceking Project Indokordsa.xlsx
```

Because the workbook is missing, no Excel-vs-SQL comparison was run and no comparison results are recorded here.

Next step:

Place the workbook at the expected path and rerun the diagnostic comparison against:

- `vw_job_order_rekap_summary`
- `vw_job_order_rekap_lines`
- `vw_job_order_po_amount_compare`
- `vw_job_order_not_yet_rop`
- `vw_job_order_excess_rop`
