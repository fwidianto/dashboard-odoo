# Odoo Connection
ODOO_URL=http://localhost:8069
ODOO_DB=odoo_db
ODOO_USERNAME=admin

# Authentication (API Key is recommended for security)
# Option 1: API Key Authentication (PREFERRED)
ODOO_API_KEY=your_api_key_here

# Option 2: Password Authentication (DEPRECATED - only use as fallback)
# ODOO_PASSWORD=your_password_here

# Odoo API Version
ODOO_API_VERSION=17

# PostgreSQL Connection
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=sync_db
POSTGRES_USER=sync_user
POSTGRES_PASSWORD=your_secure_password

# Sync Configuration
SYNC_BATCH_SIZE=1000
SYNC_MODE=incremental
SCHEDULE_INTERVAL_MINUTES=15

# Logging
LOG_LEVEL=INFO
LOG_FILE=sync.log

# Optional: API for future FastAPI integration
# API_HOST=0.0.0.0
# API_PORT=8000